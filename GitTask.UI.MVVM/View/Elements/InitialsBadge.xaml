<UserControl x:Class="GitTask.UI.MVVM.View.Elements.InitialsBadge"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        Height="25"
        Width="25">
    <Border Background="Gray" CornerRadius="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Border.ToolTip>
            <ToolTip>
                <Grid Height="30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Width="30" Height="30" Source="{Binding Avatar}"/>
                    <TextBlock Grid.Column="1" Text="{Binding Name}" Foreground="DarkSlateGray" FontSize="14" VerticalAlignment="Center" TextAlignment="Center" />
                </Grid>
            </ToolTip>
        </Border.ToolTip>
        <TextBlock Foreground="WhiteSmoke" FontSize="14" VerticalAlignment="Center" TextAlignment="Center" Margin="0,-2,0,0"
                   Text="{Binding Name, Converter={StaticResource NameToInitialsConverter}}"/>
    </Border>
</UserControl>
